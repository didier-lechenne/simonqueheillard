title: Oeuvre
icon: 🖼

columns:
  - width: 3/4
    fields:
    
      cover:
        label: Image de couverture
        type: files
        layout: list
        size: medium
        template: cover
        image:
          cover: true
          ratio: 4/3
        required: false 

      bloc:
        label: Ajouter des blocs
        type: blocks
        help: Utilisez les colonnes pour organiser vos images et textes
        fieldsets:
          - columns
          - heading
          - markdown
          - list
          - image
          - gallery
          - video
          - audio
          - spacer
          - pdf 



        default:
          - type: columns
            content:
              layout:
                - columns:
                    - width: 9/12
                    - width: 3/12
                      

  - width: 1/4
    fields:

      files:
        label: Médias
        type: files

      # info:
      #   label: Info
      #   type: info
      #   theme: negative
      #   text: A documenté.

      date:
        label: Année de création
        type: date
        default: today
        display: YYYY
        width: 1/2
        help: (cela peut être une date antérieure à la date du jour)

      # date_fin:
      #   label: Date de fin
      #   type: date
      #   default: today
      #   display: YYYY
      #   width: 1/2
      #   help: (cela peut être une date antérieure à la date du jour)

      # tags: true