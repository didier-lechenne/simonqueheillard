name: Image
icon: image
preview: image

fields:
  location:
    label: Emplacement
    type: radio
    columns: 2
    default: kirby
    options:
      kirby: Ce site web
      web: Source externe
  
  image:
    label: Image
    type: files
    query: page.images
    multiple: true
    uploads:
      template: image
    when:
      location: kirby


  
  caption:
    label: Légende
    type: writer
    inline: true
    help: Important
    when:
      location: kirby


  link:
    label: Lien
    type: url
    # help: Laissez vide pour utiliser celui du fichier
    when:
      location: web


  
  # ratio:
  #   label: Proportions
  #   type: select
  #   placeholder: Auto
  #   width: 1/2
  #   options:
  #     auto: Auto
  #     1/1: "1:1"
  #     16/9: "16:9"
  
  # crop:
  #   label: Recadrer
  #   type: toggle
  #   width: 1/2