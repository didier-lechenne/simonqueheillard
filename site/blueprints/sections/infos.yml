columns:
  main:
    width: 2/3
    sections:
      infos:
        type: pages
        label: À propos
        template: info
        parent: site.find("infos")
    
  sidebar:
    width: 1/3
    fields:
      contact:
        label: Contact
        type: writer