name: Audio
icon: file-audio
tabs:
  main:
    label: Main
    fields:
      source:
        type: files
        query: page.audio
        # query: page.audio.template('audio')
        uploads: audio
        multiple: false
        width: 1/2
      title:
        type: text
        placeholder: Title
      subtitle:
        type: writer
        icon: text
        inline: true
        placeholder: Subtitle
        marks:
          - bold
          - italic
      description:
        type: writer
        icon: text
        inline: true
        placeholder: Description
        marks:
          - bold
          - italic